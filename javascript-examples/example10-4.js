window.onload = function() {
	//Underfined variable
	var foo;
	document.write('The foo variable is ' + foo + '<br>');

	// Null varaiable
	var bar = "Hello";
	bar = null;
	document.write('The bar varaiable is ' + bar);
};
